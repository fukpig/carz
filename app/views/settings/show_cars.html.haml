.box-body.table-responsive
  %a{:href => new_settings_car_path}
    %button.btn.btn-info New make
  %br/
  %table#example2.table.table-bordered.table-striped
    %thead
      %tr
        %th Make
        %th Actions
    %tbody
      - @cars.each do |car|
        %tr
          %td= car.make
          %td 
            = link_to 'Edit', edit_settings_car_path(car)
            = link_to "Delete", delete_settings_car_path(car), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }